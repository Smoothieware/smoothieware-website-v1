# src/tools — migrated

This folder held KiCad helper scripts for the PCB render work (`extract_component_metadata.py`, `apply_drill_mask.py`). Not part of the documentation site.

Moved on 2026-09-05 to `~/dev/smoothieware/smoothieboard-pcb-renders/src/tools/`. See that project's `README.md` and this repo's `MIGRATED-PROJECTS.md`.
