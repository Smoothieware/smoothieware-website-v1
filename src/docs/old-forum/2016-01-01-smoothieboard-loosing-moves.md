# Smoothieboard loosing moves ?

*Original post date: 2016-01-01*

---

⁑

Okay, let's see. The user provided some HTML code from a forum thread and wants me to convert it into Markdown. The original HTML has three posts discussing a problem with a Smoothieboard losing moves, and the solution involved adjusting acceleration settings.

First, I need to parse the HTML structure. Each post has a title, author, date, and content. The first post is from arthurwolf suggesting checking acceleration, current, and mechanical issues. The second post from pbasic says they solved it by reducing acceleration. The third post is arthurwolf confirming that trial and error is the usual method.

I should structure the Markdown with headers for each post, using bold for the author and date, and proper indentation for the content. Also, the blockquote from the third post needs to be handled correctly. I'll make sure to preserve the conversation flow and any code snippets if present, but in this case, there's no code, just text.

Wait, the user mentioned "Markdown: /think" at the end, so maybe they want the thought process first and then the Markdown. But the initial instruction was to convert the HTML to Markdown. Let me focus on that.

Check for any nested elements or special characters that need escaping. The dates are in a specific format, so I'll keep them as is. The replies are nested under the original post, so using indentation or dashes might help. Also, the "Re: " in the titles indicates replies, so I'll use that in the headers.

Make sure the Markdown is readable, with proper line breaks and formatting. Avoid using HTML tags, just use Markdown syntax like **bold**, > for blockquotes, and lists if needed. Since there are three posts, each can be a section with the title, author, date, and content.

I think that's it. Now, structure each post with headers, authors, dates, and content, using appropriate Markdown elements.
</think>


---

### Re: Smoothieboard loosing moves ...

**arthurwolf** - 2015-06-14 14:54

That's generally how it's done yes.

---

### Re: Smoothieboard loosing moves ... solved

**pbasic** - 2015-06-14 14:54

> how do I find the highest accel value that does not leads to skipped steps ? by trial and error only ?

That's generally how it's done yes.

---

### Re: Smoothieboard loosing moves ... solved

**arthurwolf** - 2015-06-14 14:54

> how do I find the highest accel value that does not leads to skipped steps ? by trial and error only ?

That's generally how it's done yes.

---

Let me know if you'd like further formatting adjustments!

---

*This post was archived from the old Smoothieware forum.*
*Source: [Wayback Machine](https://web.archive.org/web/20150720070849/http://smoothieware.org/forum/t-1206142/smoothieboard-loosing-moves)*
